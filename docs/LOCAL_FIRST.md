# Local-First Model

ShadowProfile is designed as a local-first browser transparency tool.

The extension observes browser-visible signals, classifies them locally, and renders explanations in the popup.

Data stays on the user's device unless the user explicitly exports an artifact.
